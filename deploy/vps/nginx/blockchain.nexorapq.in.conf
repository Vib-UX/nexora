# Nitro JSON-RPC + WebSocket + Blockscout explorer behind nginx (HTTP first).
# Composed from snippets so you can reuse fragments on an existing nginx (see deploy/vps/README.md).
# After DNS points here:
#   sudo certbot --nginx -d blockchain.nexorapq.in -d ws.blockchain.nexorapq.in -d explorer.nexorapq.in

include /etc/nginx/snippets/nexora-websocket-upgrade.map.conf;
include /etc/nginx/snippets/blockchain.nexorapq.in.servers.conf;
include /etc/nginx/snippets/explorer.nexorapq.in.servers.conf;
