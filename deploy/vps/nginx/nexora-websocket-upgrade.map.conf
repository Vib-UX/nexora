# Place in http context (e.g. via include from sites-available or nginx.conf).
# Include at most once per nginx instance — duplicate map names will fail `nginx -t`.
# If another site already defines `map $http_upgrade $connection_upgrade`, omit this file.

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
